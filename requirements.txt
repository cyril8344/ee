# ===== XAU/USD Scalping Bot - Python dependencies =====
# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
websockets==12.0

# Data / market access
yfinance==0.2.40
pandas==2.2.2
numpy==1.26.4
requests==2.32.3

# Live / paper trading via MetaTrader 5 (Windows only).
# The bot runs fine without it (paper simulation fallback).
# Install manually on Windows:  pip install MetaTrader5==5.0.45
# MetaTrader5==5.0.45

# Utilities
python-dateutil==2.9.0.post0
pytz==2024.1
